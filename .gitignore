# Cloned official repos for reference/research — never committed
reference/

# SDD orchestration scratch (ledger, briefs, review packages)
.superpowers/

# Secrets and local config
.env
.env.*
# ...but the committed template carries no secrets and documents the env contract.
!reachy_companion/.env.example
*.local.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor noise
Thumbs.db
.DS_Store
